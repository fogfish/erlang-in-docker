version: 0.2

phases:
  install:
    commands:
      - echo "==> install"
      - /usr/local/bin/config.sh
  pre_build:
    commands:
      - echo "==> pre-build"
  build:
    commands:
      - echo "==> build"
      - make
  post_build:
    commands:
      - echo "==> post-build"

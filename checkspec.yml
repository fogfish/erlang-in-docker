version: 0.2

phases:
  install:
    commands:
      - echo "==> [check] install"
  pre_build:
    commands:
      - echo "==> [check] pre-build"
  build:
    commands:
      - echo "==> [check] test"
  post_build:
    commands:
      - echo "==> [check] post-build"

version: 0.2

phases:
  install:
    commands:
      - echo "==> [clean] install"
  pre_build:
    commands:
      - echo "==> [clean] pre-build"
  build:
    commands:
      - echo "==> [clean] do it"
  post_build:
    commands:
      - echo "==> [clean] post-build"
